# DocX → Image 纯 Python 渲染引擎 · 完整技术方案

> **版本**：v1.0 | **日期**：2026-07-24
> **约束**：仅支持 `.docx`（OOXML），不兼容 `.doc`（OLE2）
> **依赖**：`Pillow`（渲染）+ Python 标准库（`zipfile` / `xml.etree` / `struct` / `io` / `re`）

---

## 一、总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        docx2img CLI / API                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────┐   ┌───────────┐   ┌───────────┐   ┌──────────┐  │
│  │ ① 解包层  │──▶│ ② 解析层  │──▶│ ③ 排版层  │──▶│ ④ 渲染层 │  │
│  │  Unpacker │   │  Parser   │   │  Layout   │   │ Renderer │  │
│  └───────────┘   └───────────┘   └───────────┘   └──────────┘  │
│       │               │               │               │         │
│   zipfile         lxml/ET         自研引擎        Pillow        │
│   解压 ZIP        XML → IR        IR → 像素       像素 → PNG    │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  ⑤ 字体管理器 FontManager                                       │
│  ⑥ 样式解析器 StyleResolver                                     │
│  ⑦ 编号引擎   NumberingEngine                                   │
│  ⑧ 图片/图形  MediaResolver                                     │
│  ⑨ 公式引擎   MathRenderer (OMML)                               │
├─────────────────────────────────────────────────────────────────┤
│  ⑩ 配置 & 日志 & 测试框架                                       │
└─────────────────────────────────────────────────────────────────┘
```

### 核心数据流

```
.docx (ZIP)
  │
  ▼
DocumentModel (IR - 中间表示)
  │  ├── Section[]        页面/节
  │  ├── StyleTable       样式表
  │  ├── NumberingTable   编号定义
  │  ├── MediaStore       图片/资源
  │  └── Body[]           内容块序列
  │       ├── Paragraph
  │       │    ├── ParaProps (对齐/缩进/间距/行距)
  │       │    └── Run[]
  │       │         ├── TextRun (text + RunProps)
  │       │         ├── ImageRun (media_ref + size)
  │       │         ├── MathRun (OMML AST)
  │       │         └── BreakRun (tab / br / page_break)
  │       ├── Table
  │       │    ├── TableProps (宽度/对齐/边框)
  │       │    ├── ColGrid[]
  │       │    └── Row[] → Cell[] → Paragraph[]
  │       └── SdtBlock (内容控件)
  │
  ▼
LayoutTree (排版树)
  │  ├── Page[]
  │  │    ├── Block[]
  │  │    │    ├── LineBox[]  (段落行)
  │  │    │    │    └── GlyphBox[] (字符/图片/公式)
  │  │    │    └── TableBox
  │  │    │         └── CellBox[] → LineBox[]
  │  │    ├── HeaderBox
  │  │    └── FooterBox
  │
  ▼
Pixel Canvas (Pillow Image)
  │
  ▼
PNG / JPEG / TIFF
```

---

## 二、项目结构

```
docx2img/
├── pyproject.toml
├── requirements.txt              # Pillow>=10.0
├── README.md
│
├── src/
│   └── docx2img/
│       ├── __init__.py
│       ├── cli.py                # 命令行入口
│       ├── api.py                # Python API
│       ├── config.py             # 全局配置 (DPI/字体路径/颜色映射)
│       │
│       ├── unpack/               # ① 解包层
│       │   ├── __init__.py
│       │   └── unpacker.py       # ZIP 解压 + 文件清单
│       │
│       ├── parse/                # ② 解析层 (XML → IR)
│       │   ├── __init__.py
│       │   ├── namespaces.py     # OOXML 命名空间常量
│       │   ├── units.py          # 单位换算 (twips/EMU/half-pt/pt/px)
│       │   ├── document.py       # document.xml 主解析
│       │   ├── paragraph.py      # 段落/Run 解析
│       │   ├── table.py          # 表格解析
│       │   ├── drawing.py        # DrawingML / 图片 / 图形
│       │   ├── math_omml.py      # OMML 公式解析
│       │   ├── styles.py         # styles.xml 解析
│       │   ├── theme.py          # theme1.xml 解析
│       │   ├── numbering.py      # numbering.xml 解析
│       │   ├── section.py        # sectPr 页面/节解析
│       │   ├── header_footer.py  # header/footer XML 解析
│       │   ├── rels.py           # .rels 关系映射
│       │   └── sdt.py            # 内容控件 (Structured Document Tags)
│       │
│       ├── model/                # IR 数据模型
│       │   ├── __init__.py
│       │   ├── document.py       # DocumentModel
│       │   ├── paragraph.py      # Paragraph / Run / RunProps / ParaProps
│       │   ├── table.py          # Table / Row / Cell
│       │   ├── drawing.py        # Image / Shape / Chart
│       │   ├── math_ast.py       # 公式 AST 节点
│       │   ├── style.py          # Style / StyleTable
│       │   ├── numbering.py      # NumberingDef / LevelDef
│       │   ├── section.py        # Section / PageSetup
│       │   └── enums.py          # 枚举 (对齐/边框/编号格式/...)
│       │
│       ├── layout/               # ③ 排版层
│       │   ├── __init__.py
│       │   ├── engine.py         # 排版主引擎
│       │   ├── line_breaker.py   # 断行算法
│       │   ├── page_breaker.py   # 分页算法
│       │   ├── table_layout.py   # 表格网格计算
│       │   ├── list_layout.py    # 列表编号排版
│       │   ├── column_layout.py  # 分栏排版
│       │   ├── float_layout.py   # 浮动元素/文本环绕
│       │   ├── textbox_layout.py # 文本框排版
│       │   ├── math_layout.py    # 公式排版
│       │   ├── tab_stop.py       # 制表位计算
│       │   └── justify.py        # 两端对齐算法
│       │
│       ├── render/               # ④ 渲染层
│       │   ├── __init__.py
│       │   ├── canvas.py         # Pillow 画布封装
│       │   ├── text_renderer.py  # 文本绘制 (含字形选择)
│       │   ├── table_renderer.py # 表格绘制 (边框/背景/合并)
│       │   ├── image_renderer.py # 图片绘制
│       │   ├── shape_renderer.py # 图形绘制 (矩形/椭圆/线条/箭头)
│       │   ├── math_renderer.py  # 公式绘制
│       │   ├── border_renderer.py# 边框绘制 (单线/双线/虚线/点线)
│       │   ├── bullet_renderer.py# 列表符号绘制
│       │   └── effects.py        # 阴影/高亮/底纹
│       │
│       ├── font/                 # ⑤ 字体管理
│       │   ├── __init__.py
│       │   ├── manager.py        # 字体查找/加载/缓存
│       │   ├── fallback.py       # 字体回退链
│       │   ├── metrics.py        # 字体度量 (ascent/descent/linegap)
│       │   └── embedded.py       # 嵌入字体提取 (fontTable.xml)
│       │
│       ├── style/                # ⑥ 样式系统
│       │   ├── __init__.py
│       │   ├── resolver.py       # 样式继承链解析
│       │   ├── defaults.py       # docDefaults / 隐式默认值
│       │   └── theme_resolver.py # 主题色/主题字体解析
│       │
│       └── utils/
│           ├── __init__.py
│           ├── color.py          # 颜色解析 (hex/theme/auto)
│           ├── cache.py          # LRU 缓存
│           └── log.py            # 日志
│
├── tests/
│   ├── conftest.py
│   ├── fixtures/                 # 测试用 .docx 文件
│   │   ├── basic_text.docx
│   │   ├── styled_text.docx
│   │   ├── tables.docx
│   │   ├── images.docx
│   │   ├── headers_footers.docx
│   │   ├── lists.docx
│   │   ├── columns.docx
│   │   ├── math.docx
│   │   ├── textboxes.docx
│   │   └── complex_mixed.docx
│   ├── test_parse/
│   ├── test_layout/
│   ├── test_render/
│   └── test_e2e/
│
├── fonts/                        # 自带字体
│   ├── simsun.ttc
│   ├── simhei.ttf
│   ├── times.ttf
│   └── arial.ttf
│
└── docs/
    ├── architecture.md
    ├── ooxml_reference.md        # OOXML 结构速查
    └── iteration_plan.md         # 迭代计划
```

---

## 三、IR 数据模型（完整定义）

```python
# model/enums.py
from enum import Enum

class Alignment(Enum):
    LEFT = "left"
    CENTER = "center"
    RIGHT = "right"
    JUSTIFY = "justify"
    DISTRIBUTE = "distribute"

class BorderStyle(Enum):
    NONE = "none"
    SINGLE = "single"
    DOUBLE = "double"
    TRIPLE = "triple"
    THICK = "thick"
    DASHED = "dashed"
    DOTTED = "dotted"
    WAVE = "wave"

class NumberFormat(Enum):
    DECIMAL = "decimal"           # 1, 2, 3
    UPPER_LETTER = "upperLetter"  # A, B, C
    LOWER_LETTER = "lowerLetter"  # a, b, c
    UPPER_ROMAN = "upperRoman"    # I, II, III
    LOWER_ROMAN = "lowerRoman"    # i, ii, iii
    BULLET = "bullet"             # •
    CHINESE_COUNTING = "chineseCounting"  # 一, 二, 三
    IDEOGRAPH_DIGITAL = "ideographDigital"
    NONE = "none"

class TabStopType(Enum):
    LEFT = "left"
    CENTER = "center"
    RIGHT = "right"
    DECIMAL = "decimal"

class VerticalMerge(Enum):
    RESTART = "restart"
    CONTINUE = "continue"
    NONE = "none"

class WrapType(Enum):
    INLINE = "inline"
    SQUARE = "square"
    TIGHT = "tight"
    TOP_BOTTOM = "topAndBottom"
    BEHIND = "behind"
    IN_FRONT = "inFrontOf"

class SectionType(Enum):
    NEXT_PAGE = "nextPage"
    CONTINUOUS = "continuous"
    EVEN_PAGE = "evenPage"
    ODD_PAGE = "oddPage"
```

```python
# model/paragraph.py
from dataclasses import dataclass, field
from typing import List, Optional, Tuple
from .enums import Alignment, TabStopType

@dataclass
class TabStop:
    position: float          # pt
    type: TabStopType = TabStopType.LEFT
    leader: str = "none"     # none / dot / hyphen / underscore

@dataclass
class RunProps:
    # 字体
    font_ascii: str = "Times New Roman"
    font_east_asia: str = "SimSun"
    font_h_ansi: str = "Times New Roman"
    font_cs: str = "Times New Roman"
    font_size: float = 12.0           # pt
    # 字形
    bold: bool = False
    italic: bool = False
    underline: bool = False
    underline_style: str = "single"
    strike: bool = False
    double_strike: bool = False
    # 颜色
    color: Tuple[int, int, int] = (0, 0, 0)
    highlight: Optional[str] = None    # yellow / green / cyan ...
    shading: Optional[str] = None      # 背景色 hex
    # 位置
    vertical_align: str = "baseline"   # baseline / superscript / subscript
    position_offset: float = 0.0       # pt, 正=上移 负=下移
    # 缩放/间距
    scale: int = 100                   # 百分比
    spacing: float = 0.0              # pt, 字符间距
    # 其他
    small_caps: bool = False
    all_caps: bool = False
    kern: float = 0.0                 # pt
    rtl: bool = False                 # 从右到左

@dataclass
class ParaProps:
    alignment: Alignment = Alignment.LEFT
    space_before: float = 0.0         # pt
    space_after: float = 0.0          # pt
    line_spacing: float = 1.0         # 倍数 (auto)
    line_spacing_exact: Optional[float] = None  # pt (exact/atLeast)
    line_spacing_rule: str = "auto"   # auto / exact / atLeast
    indent_left: float = 0.0          # pt
    indent_right: float = 0.0         # pt
    first_line_indent: float = 0.0    # pt
    hanging_indent: float = 0.0       # pt
    tab_stops: List[TabStop] = field(default_factory=list)
    keep_next: bool = False
    keep_lines: bool = False
    page_break_before: bool = False
    widow_control: bool = True
    outline_level: Optional[int] = None
    # 编号
    num_id: Optional[int] = None
    num_level: int = 0
    # 边框
    borders: dict = field(default_factory=dict)
    shading: Optional[str] = None
    # 样式引用
    style_id: str = ""
    # 制表位
    default_tab_stop: float = 36.0    # pt

@dataclass
class TextRun:
    text: str
    props: RunProps = field(default_factory=RunProps)

@dataclass
class ImageRun:
    media_ref: str                     # rId
    data: Optional[bytes] = None
    width_emu: int = 0
    height_emu: int = 0
    wrap_type: str = "inline"
    # 浮动定位 (非 inline 时)
    pos_x: Optional[float] = None
    pos_y: Optional[float] = None
    relative_x: str = "column"
    relative_y: str = "paragraph"

@dataclass
class MathRun:
    ast: object = None                 # MathNode AST

@dataclass
class BreakRun:
    break_type: str = "line"           # line / page / column / textWrapping

@dataclass
class TabRun:
    pass

@dataclass
class Run:
    """Run 联合体"""
    text: Optional[TextRun] = None
    image: Optional[ImageRun] = None
    math: Optional[MathRun] = None
    brk: Optional[BreakRun] = None
    tab: Optional[TabRun] = None

@dataclass
class Paragraph:
    runs: List[Run] = field(default_factory=list)
    props: ParaProps = field(default_factory=ParaProps)
```

```python
# model/table.py
from dataclasses import dataclass, field
from typing import List, Optional
from .paragraph import Paragraph
from .enums import BorderStyle, VerticalMerge

@dataclass
class BorderDef:
    style: BorderStyle = BorderStyle.SINGLE
    width: float = 0.5         # pt (sz/8)
    color: tuple = (0, 0, 0)
    space: float = 0.0         # pt

@dataclass
class CellProps:
    width: float = 0.0         # pt
    width_type: str = "dxa"    # dxa / pct / auto
    grid_span: int = 1
    v_merge: VerticalMerge = VerticalMerge.NONE
    shading: Optional[str] = None
    borders: dict = field(default_factory=dict)  # top/bottom/left/right
    vertical_align: str = "top"  # top / center / bottom
    margins: dict = field(default_factory=dict)  # top/bottom/left/right pt
    no_wrap: bool = False

@dataclass
class Cell:
    paragraphs: List[Paragraph] = field(default_factory=list)
    props: CellProps = field(default_factory=CellProps)

@dataclass
class Row:
    cells: List[Cell] = field(default_factory=list)
    height: float = 0.0        # pt
    height_rule: str = "atLeast"  # auto / atLeast / exact
    is_header: bool = False
    cant_split: bool = False

@dataclass
class TableProps:
    width: float = 0.0         # pt
    width_type: str = "dxa"
    alignment: str = "left"
    indent: float = 0.0        # pt
    borders: dict = field(default_factory=dict)
    cell_spacing: float = 0.0  # pt
    layout: str = "fixed"      # fixed / autofit
    style_id: str = ""

@dataclass
class Table:
    rows: List[Row] = field(default_factory=list)
    col_widths: List[float] = field(default_factory=list)  # pt
    props: TableProps = field(default_factory=TableProps)
```

```python
# model/section.py
from dataclasses import dataclass, field
from typing import List, Optional
from .enums import SectionType
from .paragraph import Paragraph

@dataclass
class ColumnDef:
    width: float = 0.0         # pt
    space: float = 0.0         # pt

@dataclass
class Section:
    # 页面
    page_w: float = 595.0      # pt (A4)
    page_h: float = 842.0
    margin_top: float = 72.0
    margin_bottom: float = 72.0
    margin_left: float = 90.0
    margin_right: float = 90.0
    header_distance: float = 36.0
    footer_distance: float = 36.0
    gutter: float = 0.0
    orientation: str = "portrait"  # portrait / landscape
    # 分栏
    columns: List[ColumnDef] = field(default_factory=list)
    col_equal_width: bool = True
    col_count: int = 1
    col_space: float = 36.0    # pt
    col_sep: bool = False      # 栏间分隔线
    # 节类型
    section_type: SectionType = SectionType.NEXT_PAGE
    # 页眉页脚引用
    header_refs: dict = field(default_factory=dict)  # type → rId
    footer_refs: dict = field(default_factory=dict)
    # 页码
    page_num_start: Optional[int] = None
    # 行号
    line_numbers: bool = False
```

```python
# model/document.py
from dataclasses import dataclass, field
from typing import List, Dict, Union
from .paragraph import Paragraph
from .table import Table
from .section import Section
from .style import StyleTable
from .numbering import NumberingTable

@dataclass
class DocumentModel:
    body: List[Union[Paragraph, Table]] = field(default_factory=list)
    sections: List[Section] = field(default_factory=list)
    styles: StyleTable = field(default_factory=StyleTable)
    numbering: NumberingTable = field(default_factory=NumberingTable)
    media: Dict[str, bytes] = field(default_factory=dict)
    headers: Dict[str, List] = field(default_factory=dict)  # rId → body
    footers: Dict[str, List] = field(default_factory=dict)
    default_run_props: object = None
    default_para_props: object = None
    theme_colors: dict = field(default_factory=dict)
    theme_fonts: dict = field(default_factory=dict)
```

---

## 四、迭代路线图（8 个阶段）

```
P0 ─── P1 ─── P2 ─── P3 ─── P4 ─── P5 ─── P6 ─── P7
基础    样式   表格   图片   页眉    列表   高级   公式
文本    系统   完整   图形   页脚    编号   排版   OMML
                        分节
                        分栏

每阶段 ≈ 1~2 周（单人全职）
```

---

### P0 · 基础文本渲染（第 1~2 周）

**目标**：能正确渲染纯文本 `.docx`，输出 PNG。

#### 涉及 OOXML 结构

```xml
word/document.xml
├── w:body
│   ├── w:p                        段落
│   │   ├── w:pPr                  段落属性
│   │   │   ├── w:jc               对齐
│   │   │   ├── w:spacing          间距/行距
│   │   │   ├── w:ind              缩进
│   │   │   └── w:pStyle           样式引用 (P0 仅记录,不解析)
│   │   └── w:r                    Run
│   │       ├── w:rPr              Run 属性
│   │       │   ├── w:rFonts       字体
│   │       │   ├── w:sz / w:szCs  字号
│   │       │   ├── w:b / w:i      加粗/斜体
│   │       │   ├── w:u            下划线
│   │       │   ├── w:strike       删除线
│   │       │   ├── w:color        颜色
│   │       │   ├── w:highlight    高亮
│   │       │   ├── w:vertAlign    上标/下标
│   │       │   ├── w:smallCaps    小型大写字母
│   │       │   ├── w:spacing      字符间距
│   │       │   └── w:position     基线偏移
│   │       ├── w:t                文本
│   │       ├── w:tab              制表符
│   │       └── w:br               换行/分页
│   └── w:sectPr                   页面设置
│       ├── w:pgSz                 纸张大小
│       └── w:pgMar                页边距
```

#### 核心算法：断行

```python
# layout/line_breaker.py

class LineBreaker:
    """
    断行策略：
    1. 英文：按空格/连字符断词 (word-level)
    2. 中文/日文/韩文：任意字符间断行 (char-level)
    3. 混合：CJK 字符间可断，拉丁词内不可断
    4. 标点禁则：行首禁止出现 ，。、；：！？）】》
                 行尾禁止出现 （【《
    """

    # CJK Unicode 范围
    CJK_RANGES = [
        (0x4E00, 0x9FFF),    # CJK 统一汉字
        (0x3400, 0x4DBF),    # CJK 扩展 A
        (0x3000, 0x303F),    # CJK 标点
        (0xFF00, 0xFFEF),    # 全角字符
        (0x3040, 0x309F),    # 平假名
        (0x30A0, 0x30FF),    # 片假名
        (0xAC00, 0xD7AF),    # 韩文音节
    ]

    NO_START_CHARS = set("，。、；：！？）】》」』〉,.;:!?)")
    NO_END_CHARS = set("（【《「『〈(")

    def is_cjk(self, ch: str) -> bool:
        cp = ord(ch)
        return any(lo <= cp <= hi for lo, hi in self.CJK_RANGES)

    def can_break_before(self, ch: str) -> bool:
        """该字符能否出现在行首"""
        return ch not in self.NO_START_CHARS

    def can_break_after(self, ch: str) -> bool:
        """该字符后能否断行"""
        return ch not in self.NO_END_CHARS

    def break_line(self, runs: list, max_width: int,
                   font_mgr, measure_fn) -> list:
        """
        输入：Run 序列 + 可用宽度
        输出：[Line] 列表

        算法：
        1. 将 Run 展开为 (char, font, props) 序列
        2. 贪心扫描，累加宽度
        3. 遇到可断点且超宽时断行
        4. 处理标点禁则（回溯/前推）
        """
        ...
```

#### 核心算法：分页

```python
# layout/page_breaker.py

class PageBreaker:
    """
    分页规则：
    1. 内容超出页面可用高度 → 分页
    2. w:pageBreakBefore → 段前强制分页
    3. w:br type="page" → 行内强制分页
    4. w:keepNext → 段落与下一段保持同页
    5. w:keepLines → 段落内所有行保持同页
    6. w:widowControl → 孤行控制（至少2行在同页）
    7. 表格行：w:cantSplit → 行不可跨页拆分
    """

    def paginate(self, blocks: list, page_height: int,
                 margin_top: int, margin_bottom: int) -> list:
        ...
```

#### 渲染：文本绘制

```python
# render/text_renderer.py
from PIL import Image, ImageDraw, ImageFont

class TextRenderer:
    def __init__(self, canvas: Image.Image):
        self.draw = ImageDraw.Draw(canvas)

    def draw_run(self, run, x: int, y: int, font: ImageFont):
        """
        绘制单个 Run：
        1. 高亮背景 → 先画矩形
        2. 文本 → draw.text()
        3. 下划线 → draw.line() 在 baseline + descent 处
        4. 删除线 → draw.line() 在 x-height/2 处
        5. 上标/下标 → y 偏移
        """
        props = run.props

        # 高亮
        if props.highlight:
            hl_color = HIGHLIGHT_COLORS.get(props.highlight, (255, 255, 0))
            bbox = font.getbbox(run.text)
            w = bbox[2] - bbox[0]
            h = bbox[3] - bbox[1]
            self.draw.rectangle([x, y, x + w, y + h], fill=hl_color)

        # 文本
        self.draw.text((x, y), run.text, font=font, fill=props.color)

        # 下划线
        if props.underline:
            bbox = font.getbbox(run.text)
            tw = bbox[2] - bbox[0]
            # baseline 位置 ≈ ascent
            ascent = font.getmetrics()[0]
            uy = y + ascent + 1
            self.draw.line([(x, uy), (x + tw, uy)],
                          fill=props.color, width=max(1, int(props.font_size / 12)))

        # 删除线
        if props.strike:
            bbox = font.getbbox(run.text)
            tw = bbox[2] - bbox[0]
            ascent = font.getmetrics()[0]
            sy = y + ascent // 2
            self.draw.line([(x, sy), (x + tw, sy)],
                          fill=props.color, width=1)
```

#### P0 验收标准

| 测试项 | 预期 |
|--------|------|
| 纯英文段落 | 正确断行、对齐 |
| 纯中文段落 | 逐字断行、无乱码 |
| 中英混排 | 英文词不断开，中文可断 |
| 加粗/斜体/下划线/删除线 | 视觉正确 |
| 字号 8pt~72pt | 尺寸正确 |
| 颜色/高亮 | 颜色正确 |
| 上标/下标 | 位置偏移正确 |
| 左/中/右对齐 | 位置正确 |
| 首行缩进 2 字符 | 缩进正确 |
| 段前/段后间距 | 间距正确 |
| 行距 1.0/1.5/2.0/固定值 | 行高正确 |
| 强制分页 (Ctrl+Enter) | 正确分页 |
| A4 / Letter / 自定义纸张 | 页面尺寸正确 |
| 横向页面 | 宽高互换 |

---

### P1 · 样式系统（第 3 周）

**目标**：完整支持 `styles.xml` + `theme1.xml` 的样式继承。

#### 涉及文件

```
word/styles.xml
├── w:docDefaults
│   ├── w:rPrDefault → w:rPr     全局默认 Run 属性
│   └── w:pPrDefault → w:pPr     全局默认段落属性
├── w:style (type="paragraph")    段落样式
│   ├── w:styleId
│   ├── w:name
│   ├── w:basedOn                 继承父样式
│   ├── w:next                    下一段默认样式
│   ├── w:pPr
│   └── w:rPr
├── w:style (type="character")    字符样式
├── w:style (type="table")        表格样式
└── w:style (type="numbering")    编号样式

word/theme/theme1.xml
├── a:clrScheme                   主题色 (dk1/lt1/dk2/lt2/accent1~6/hlink/folHlink)
├── a:fontScheme                  主题字体 (majorFont/minorFont)
└── a:fmtScheme                   格式方案
```

#### 样式解析优先级（从低到高）

```
docDefaults (全局默认)
  ↓ 覆盖
basedOn 链 (祖父样式 → 父样式)
  ↓ 覆盖
当前样式 (w:pStyle / w:rStyle 引用)
  ↓ 覆盖
直接格式 (段落/Run 上的 w:pPr / w:rPr)
```

#### 核心实现

```python
# style/resolver.py

class StyleResolver:
    def __init__(self, style_table: StyleTable, theme: dict,
                 default_rpr: RunProps, default_ppr: ParaProps):
        self.styles = style_table
        self.theme = theme
        self.default_rpr = default_rpr
        self.default_ppr = default_ppr
        self._cache = {}

    def resolve_para(self, style_id: str, direct: ParaProps) -> ParaProps:
        """解析段落最终属性"""
        chain = self._build_chain(style_id)
        result = self._clone(self.default_ppr)
        for sid in chain:
            style = self.styles.get(sid)
            if style and style.ppr:
                result = self._merge_ppr(result, style.ppr)
        result = self._merge_ppr(result, direct)
        return result

    def resolve_run(self, style_id: str, para_style_id: str,
                    direct: RunProps) -> RunProps:
        """解析 Run 最终属性"""
        result = self._clone(self.default_rpr)
        # 段落样式中的 rPr
        chain = self._build_chain(para_style_id)
        for sid in chain:
            style = self.styles.get(sid)
            if style and style.rpr:
                result = self._merge_rpr(result, style.rpr)
        # 字符样式
        if style_id:
            chain2 = self._build_chain(style_id)
            for sid in chain2:
                style = self.styles.get(sid)
                if style and style.rpr:
                    result = self._merge_rpr(result, style.rpr)
        # 直接格式
        result = self._merge_rpr(result, direct)
        # 主题字体替换
        result = self._apply_theme_fonts(result)
        return result

    def _build_chain(self, style_id: str) -> list:
        """构建 basedOn 继承链（从根到叶）"""
        chain = []
        visited = set()
        sid = style_id
        while sid and sid not in visited:
            visited.add(sid)
            chain.append(sid)
            style = self.styles.get(sid)
            sid = style.based_on if style else None
        chain.reverse()
        return chain

    def _apply_theme_fonts(self, props: RunProps) -> RunProps:
        """将 +mj-lt / +mn-lt / +mj-ea / +mn-ea 替换为主题字体"""
        theme_map = {
            "+mj-lt": self.theme.get("major_latin", "Cambria"),
            "+mn-lt": self.theme.get("minor_latin", "Calibri"),
            "+mj-ea": self.theme.get("major_ea", ""),
            "+mn-ea": self.theme.get("minor_ea", ""),
        }
        if props.font_ascii in theme_map:
            props.font_ascii = theme_map[props.font_ascii]
        if props.font_east_asia in theme_map:
            props.font_east_asia = theme_map[props.font_east_asia]
        return props
```

#### 主题色解析

```python
# style/theme_resolver.py

class ThemeResolver:
    """
    解析 theme1.xml 中的 a:clrScheme
    颜色引用方式：
    - 直接 hex: w:color val="FF0000"
    - 主题色:   w:color w:themeColor="accent1"
    - 主题色+变换: w:color w:themeColor="accent1" w:themeTint="BF"
    - 主题色+明暗: w:color w:themeColor="dk1" w:themeShade="80"
    """

    def resolve_color(self, hex_val=None, theme_color=None,
                      tint=None, shade=None) -> tuple:
        if hex_val and hex_val != 'auto':
            return self._hex_to_rgb(hex_val)
        if theme_color:
            base = self.theme_colors.get(theme_color, (0, 0, 0))
            if tint:
                base = self._apply_tint(base, int(tint, 16) / 255.0)
            if shade:
                base = self._apply_shade(base, int(shade, 16) / 255.0)
            return base
        return (0, 0, 0)

    def _apply_tint(self, rgb, factor):
        """向白色混合"""
        return tuple(int(c + (255 - c) * factor) for c in rgb)

    def _apply_shade(self, rgb, factor):
        """向黑色混合"""
        return tuple(int(c * factor) for c in rgb)
```

#### P1 验收标准

| 测试项 | 预期 |
|--------|------|
| Heading 1~9 样式 | 字号/加粗/颜色/间距正确 |
| Normal 样式修改 | 全文默认字体变化 |
| basedOn 三级继承 | 属性正确继承+覆盖 |
| 主题色 accent1~6 | 颜色正确 |
| 主题字体 majorFont/minorFont | 字体正确 |
| 字符样式 (如 Emphasis) | 斜体等正确 |
| 直接格式覆盖样式 | 优先级正确 |

---

### P2 · 表格完整支持（第 4~5 周）

**目标**：支持复杂表格，包括合并单元格、嵌套表格、表格样式。

#### 涉及 OOXML 结构

```xml
w:tbl
├── w:tblPr
│   ├── w:tblW              表格宽度
│   ├── w:jc                表格对齐
│   ├── w:tblInd            表格缩进
│   ├── w:tblBorders        表格边框 (6边)
│   ├── w:tblCellSpacing    单元格间距
│   ├── w:tblLayout         fixed / autofit
│   ├── w:tblStyle          表格样式引用
│   └── w:tblLook           条件格式开关
├── w:tblGrid
│   └── w:gridCol[]         列定义
├── w:tr
│   ├── w:trPr
│   │   ├── w:trHeight      行高
│   │   ├── w:tblHeader     表头行
│   │   ├── w:cantSplit     不可跨页
│   │   └── w:trStyle
│   └── w:tc
│       ├── w:tcPr
│       │   ├── w:tcW       单元格宽度
│       │   ├── w:gridSpan  水平合并
│       │   ├── w:vMerge    垂直合并
│       │   ├── w:tcBorders 单元格边框
│       │   ├── w:shd       背景色
│       │   ├── w:vAlign    垂直对齐
│       │   ├── w:tcMar     单元格边距
│       │   └── w:noWrap    禁止换行
│       ├── w:p[]           单元格内容（段落）
│       └── w:tbl           嵌套表格！
```

#### 核心算法：表格网格计算

```python
# layout/table_layout.py

class TableLayoutEngine:
    """
    表格布局算法（参考 CSS 表格布局 + OOXML 规范）：

    1. 确定列宽
       - fixed 布局：直接使用 tblGrid 中的 gridCol 宽度
       - autofit 布局：根据内容计算（类似 CSS auto）
       - 百分比宽度：按表格总宽度换算

    2. 处理合并
       - gridSpan：水平合并，占多列
       - vMerge restart/continue：垂直合并
       - 构建逻辑网格 → 物理网格映射

    3. 计算行高
       - 每行高度 = max(所有单元格内容高度, trHeight)
       - heightRule: auto / atLeast / exact

    4. 跨页拆分
       - cantSplit 行不拆分
       - tblHeader 行在每页重复
       - 其他行可在行间拆分
    """

    def layout(self, table: Table, available_width: int,
               font_mgr, para_layout_fn) -> TableBox:
        # Step 1: 列宽计算
        col_widths = self._calc_col_widths(table, available_width)

        # Step 2: 构建网格（处理合并）
        grid = self._build_grid(table, col_widths)

        # Step 3: 逐单元格排版
        for row_idx, row in enumerate(table.rows):
            for cell_idx, cell in enumerate(row.cells):
                cell_box = self._layout_cell(
                    cell, col_widths, grid, para_layout_fn)
                grid[row_idx][cell_idx].content = cell_box

        # Step 4: 计算行高
        row_heights = self._calc_row_heights(grid, table.rows)

        # Step 5: 垂直对齐
        self._apply_vertical_align(grid, row_heights)

        return TableBox(grid=grid, col_widths=col_widths,
                       row_heights=row_heights)

    def _build_grid(self, table, col_widths):
        """
        处理 gridSpan 和 vMerge，构建二维网格
        返回 grid[row][col] = CellBox | None (被合并覆盖)
        """
        n_rows = len(table.rows)
        n_cols = len(col_widths)
        grid = [[None] * n_cols for _ in range(n_rows)]

        for r, row in enumerate(table.rows):
            c = 0
            for cell in row.cells:
                # 跳过被垂直合并占据的位置
                while c < n_cols and grid[r][c] is not None:
                    c += 1
                if c >= n_cols:
                    break

                span = cell.props.grid_span
                # 水平合并
                for sc in range(span):
                    if c + sc < n_cols:
                        grid[r][c + sc] = CellBox(
                            cell=cell, col_start=c, col_span=span,
                            is_origin=(sc == 0))

                # 垂直合并
                if cell.props.v_merge == VerticalMerge.RESTART:
                    # 向下扫描 continue
                    rr = r + 1
                    while rr < n_rows:
                        next_cell = self._get_cell_at(table, rr, c)
                        if next_cell and next_cell.props.v_merge == VerticalMerge.CONTINUE:
                            for sc in range(span):
                                if c + sc < n_cols:
                                    grid[rr][c + sc] = CellBox(
                                        cell=next_cell, col_start=c,
                                        col_span=span, is_origin=False,
                                        v_merged=True)
                            rr += 1
                        else:
                            break

                c += span

        return grid
```

#### 表格边框绘制

```python
# render/border_renderer.py

class BorderRenderer:
    """
    支持的边框样式：
    - single:  单线 ─
    - double:  双线 ═
    - triple:  三线
    - thick:   粗线 ━
    - dashed:  虚线 - - -
    - dotted:  点线 · · ·
    - wave:    波浪线
    - nil:     无边框

    边框冲突解决（优先级从高到低）：
    1. 单元格边框 > 表格边框
    2. 较宽的边框 > 较窄的
    3. 显式设置 > 继承
    """

    def draw_border(self, draw, x1, y1, x2, y2, border: BorderDef):
        if border.style == BorderStyle.NONE:
            return
        w = max(1, int(border.width))
        color = border.color

        if border.style == BorderStyle.SINGLE:
            draw.line([(x1, y1), (x2, y2)], fill=color, width=w)

        elif border.style == BorderStyle.DOUBLE:
            gap = max(1, w)
            if y1 == y2:  # 水平线
                draw.line([(x1, y1 - gap), (x2, y2 - gap)], fill=color, width=1)
                draw.line([(x1, y1 + gap), (x2, y2 + gap)], fill=color, width=1)
            else:  # 垂直线
                draw.line([(x1 - gap, y1), (x2 - gap, y2)], fill=color, width=1)
                draw.line([(x1 + gap, y1), (x2 + gap, y2)], fill=color, width=1)

        elif border.style == BorderStyle.DASHED:
            self._draw_dashed(draw, x1, y1, x2, y2, color, w, dash=6, gap=4)

        elif border.style == BorderStyle.DOTTED:
            self._draw_dashed(draw, x1, y1, x2, y2, color, w, dash=2, gap=3)
        ...
```

#### P2 验收标准

| 测试项 | 预期 |
|--------|------|
| 基础表格 (3×3) | 网格正确 |
| 水平合并 (gridSpan) | 跨列正确 |
| 垂直合并 (vMerge) | 跨行正确 |
| 合并组合 (L形/T形) | 正确 |
| 嵌套表格 | 递归渲染 |
| 表格边框 (6种样式) | 视觉正确 |
| 单元格背景色 | 正确 |
| 单元格垂直对齐 | top/center/bottom |
| 表格对齐 (left/center/right) | 位置正确 |
| 表格跨页 | 正确分页 |
| 表头行重复 | 每页显示 |
| 不可拆分行 | 整体移动 |
| 表格样式 (TableGrid等) | 样式正确 |
| 单元格边距 | 内边距正确 |

---

### P3 · 图片与图形 + 分节分栏（第 6~7 周）

#### 3A：图片

```xml
w:r → w:drawing
├── wp:inline                    行内图片
│   ├── wp:extent cx/cy         尺寸 (EMU)
│   └── a:graphic
│       └── pic:pic
│           └── pic:blipFill
│               └── a:blip r:embed="rId5"
├── wp:anchor                    浮动图片
│   ├── wp:positionH / V        定位
│   ├── wp:wrapSquare           环绕方式
│   ├── wp:wrapTight
│   ├── wp:wrapTopAndBottom
│   └── wp:wrapNone
```

```python
# parse/drawing.py

class DrawingParser:
    def parse(self, drawing_el) -> ImageRun:
        # 1. 判断 inline / anchor
        inline = drawing_el.find(f"{{{NS['wp']}}}inline")
        anchor = drawing_el.find(f"{{{NS['wp']}}}anchor")

        if inline is not None:
            return self._parse_inline(inline)
        elif anchor is not None:
            return self._parse_anchor(anchor)

    def _parse_inline(self, el) -> ImageRun:
        extent = el.find(f"{{{NS['wp']}}}extent")
        cx = int(extent.get('cx', 0))
        cy = int(extent.get('cy', 0))

        blip = el.find(f".//{{{NS['a']}}}blip")
        rid = blip.get(f"{{{NS['r']}}}embed", '')

        return ImageRun(
            media_ref=rid,
            width_emu=cx, height_emu=cy,
            wrap_type="inline",
        )

    def _parse_anchor(self, el) -> ImageRun:
        # 解析浮动定位 + 环绕方式
        pos_h = el.find(f"{{{NS['wp']}}}positionH")
        pos_v = el.find(f"{{{NS['wp']}}}positionV")
        # ... 解析 relativeFrom, align, posOffset
        # ... 解析 wrapSquare / wrapTight / wrapTopAndBottom / wrapNone
        ...
```

#### 3B：分节与分栏

```xml
w:sectPr
├── w:type val="continuous"      节类型
├── w:pgSz / w:pgMar            页面
├── w:cols                       分栏
│   ├── w:col                   栏定义
│   │   ├── w:w                 栏宽
│   │   └── w:space             栏间距
│   ├── w:num                   栏数
│   ├── w:space                 默认栏距
│   ├── w:sep                   栏间分隔线
│   └── w:equalWidth            等宽
├── w:headerReference           页眉引用
├── w:footerReference           页脚引用
└── w:lnNumType                 行号
```

```python
# layout/column_layout.py

class ColumnLayoutEngine:
    """
    分栏排版算法：
    1. 计算每栏宽度 = (可用宽度 - (n-1)*栏距) / n
    2. 内容先排第一栏，满了排第二栏...
    3. 栏间分隔线 (w:sep)
    4. 连续分节 (continuous)：同一页内切换栏数
    5. 平衡栏：最后一栏内容均分（可选）
    """

    def layout_columns(self, blocks, section, page_area):
        n = section.col_count
        col_w = (page_area.width - (n - 1) * section.col_space) / n
        columns = [[] for _ in range(n)]
        col_idx = 0
        cursor_y = page_area.top

        for block in blocks:
            block_height = block.total_height
            if cursor_y + block_height > page_area.bottom:
                col_idx += 1
                cursor_y = page_area.top
                if col_idx >= n:
                    # 需要新页
                    yield columns
                    columns = [[] for _ in range(n)]
                    col_idx = 0

            columns[col_idx].append((block, cursor_y))
            cursor_y += block_height

        yield columns
```

#### P3 验收标准

| 测试项 | 预期 |
|--------|------|
| 行内图片 | 尺寸正确、随文字流动 |
| 浮动图片 (square wrap) | 文字环绕 |
| 浮动图片 (top/bottom) | 上下不环绕 |
| 浮动图片 (behind/inFront) | 层叠正确 |
| 图片定位 (绝对/相对) | 位置正确 |
| 多节文档 | 每节独立页面设置 |
| 2/3 栏分栏 | 栏宽正确 |
| 栏间分隔线 | 显示正确 |
| 连续分节 | 同页切换栏数 |
| 不同节不同纸张 | 正确 |

---

### P4 · 页眉页脚 + 页码（第 8 周）

```xml
word/header1.xml / footer1.xml
├── w:p[]                       与 document.xml 相同的段落结构
├── w:fldSimple                 简单域
│   └── w:instr " PAGE "       页码
├── w:r → w:fldChar            复杂域
│   ├── w:fldChar type="begin"
│   ├── w:instrText " PAGE "
│   └── w:fldChar type="end"
└── w:sdt                       内容控件

引用方式 (sectPr):
├── w:headerReference type="default" r:id="rId7"
├── w:headerReference type="first" r:id="rId8"
├── w:headerReference type="even" r:id="rId9"
├── w:footerReference type="default" r:id="rId10"
└── w:titlePg                   首页不同
```

```python
# parse/header_footer.py

class HeaderFooterParser:
    """
    页眉页脚类型：
    - default: 默认（奇数页）
    - first:   首页
    - even:    偶数页

    域代码支持：
    - PAGE:    当前页码
    - NUMPAGES: 总页数
    - DATE:    日期
    - AUTHOR:  作者
    - TITLE:   标题
    - SECTION: 节号
    """

    def parse_field(self, instr_text: str) -> str:
        instr = instr_text.strip().upper()
        if instr == "PAGE":
            return "{page_num}"       # 占位符，渲染时替换
        elif instr == "NUMPAGES":
            return "{total_pages}"
        elif instr.startswith("DATE"):
            return datetime.now().strftime("%Y-%m-%d")
        ...
```

#### P4 验收标准

| 测试项 | 预期 |
|--------|------|
| 默认页眉/页脚 | 每页显示 |
| 首页不同 | 首页独立 |
| 奇偶页不同 | 交替显示 |
| 页码 (PAGE) | 数字正确 |
| 总页数 (NUMPAGES) | 数字正确 |
| "第 X 页 共 Y 页" | 格式正确 |
| 页眉中的图片 | 正确显示 |
| 页眉中的表格 | 正确显示 |
| 不同节不同页眉 | 正确切换 |

---

### P5 · 列表编号（第 9 周）

```xml
word/numbering.xml
├── w:abstractNum abstractNumId="0"
│   ├── w:multiLevelType val="hybridMultilevel"
│   └── w:lvl ilvl="0"
│       ├── w:start val="1"
│       ├── w:numFmt val="decimal"
│       ├── w:lvlText val="%1."
│       ├── w:lvlJc val="left"
│       ├── w:pPr
│       │   ├── w:ind w:left="720" w:hanging="360"
│       │   └── w:tabs
│       └── w:rPr
│           └── w:rFonts w:hint="default"
├── w:num numId="1"
│   └── w:abstractNumId val="0"

document.xml 中引用：
w:pPr → w:numPr
├── w:ilvl val="0"              级别
└── w:numId val="1"             编号实例
```

```python
# layout/list_layout.py

class NumberingEngine:
    """
    编号格式支持：
    - decimal:        1. 2. 3.
    - upperLetter:    A. B. C.
    - lowerLetter:    a. b. c.
    - upperRoman:     I. II. III.
    - lowerRoman:     i. ii. iii.
    - bullet:         • / ○ / ■ / ➤
    - chineseCounting: 一、二、三、
    - ideographDigital: 甲、乙、丙、
    - decimalZero:    01. 02. 03.

    多级列表：
    - lvlText="%1.%2" → 1.1, 1.2, 2.1
    - 每级独立缩进

    编号计数器：
    - 每个 numId 独立计数
    - 级别变化时重置子级
    """

    def __init__(self, numbering_table: NumberingTable):
        self.table = numbering_table
        self.counters = {}  # numId → {level: current_count}

    def get_label(self, num_id: int, level: int) -> str:
        defn = self.table.get_definition(num_id)
        if not defn:
            return ""

        lvl = defn.levels.get(level)
        if not lvl:
            return ""

        # 更新计数器
        if num_id not in self.counters:
            self.counters[num_id] = {}
        ctr = self.counters[num_id]
        ctr[level] = ctr.get(level, lvl.start - 1) + 1
        # 重置更深层级
        for l in list(ctr.keys()):
            if l > level:
                del ctr[l]

        # 格式化
        num = ctr[level]
        fmt = lvl.num_fmt

        if fmt == NumberFormat.DECIMAL:
            text = str(num)
        elif fmt == NumberFormat.UPPER_LETTER:
            text = self._to_letter(num).upper()
        elif fmt == NumberFormat.LOWER_LETTER:
            text = self._to_letter(num).lower()
        elif fmt == NumberFormat.UPPER_ROMAN:
            text = self._to_roman(num).upper()
        elif fmt == NumberFormat.LOWER_ROMAN:
            text = self._to_roman(num).lower()
        elif fmt == NumberFormat.BULLET:
            text = lvl.bullet_char or "•"
        elif fmt == NumberFormat.CHINESE_COUNTING:
            text = self._to_chinese(num)
        else:
            text = str(num)

        # 替换 lvlText 中的 %1, %2 ...
        result = lvl.lvl_text
        for i in range(level + 1):
            placeholder = f"%{i + 1}"
            if placeholder in result:
                n = ctr.get(i, 1)
                result = result.replace(placeholder, self._format_num(n, defn.levels[i].num_fmt))

        return result

    def _to_roman(self, num: int) -> str:
        vals = [1000,900,500,400,100,90,50,40,10,9,5,4,1]
        syms = ['m','cm','d','cd','c','xc','l','xl','x','ix','v','iv','i']
        result = ''
        for v, s in zip(vals, syms):
            while num >= v:
                result += s
                num -= v
        return result

    def _to_chinese(self, num: int) -> str:
        chars = '零一二三四五六七八九十'
        if num <= 10:
            return chars[num]
        # 简化处理...
        return str(num)
```

#### P5 验收标准

| 测试项 | 预期 |
|--------|------|
| 有序列表 1. 2. 3. | 编号正确 |
| 无序列表 • | 符号正确 |
| 多级列表 1.1 / 1.1.1 | 层级正确 |
| 罗马数字 I. II. III. | 正确 |
| 字母 A. B. C. | 正确 |
| 中文编号 一、二、三 | 正确 |
| 悬挂缩进 | 文本对齐正确 |
| 编号字体/颜色自定义 | 正确 |
| 列表重新开始 | 计数重置 |

---

### P6 · 高级排版（第 10~11 周）

#### 6A：两端对齐

```python
# layout/justify.py

class JustifyEngine:
    """
    两端对齐算法：
    1. 计算行内所有字符总宽度
    2. 计算剩余空间 = 可用宽度 - 总宽度
    3. 将剩余空间均匀分配到字符间
    4. CJK 字符间和拉丁词间都分配
    5. 最后一行不两端对齐（左对齐）
    6. 只有一个词的行不两端对齐

    分散对齐 (distribute)：
    - 与 justify 类似，但最后一个字符后也分配空间
    """

    def distribute(self, runs: list, available_width: int,
                   is_last_line: bool) -> list:
        if is_last_line or len(runs) <= 1:
            return runs  # 不处理

        total_w = sum(r.width for r in runs)
        gap = available_width - total_w
        if gap <= 0:
            return runs

        n_gaps = len(runs) - 1
        extra_per_gap = gap / n_gaps

        x = runs[0].x
        for i, r in enumerate(runs):
            r.x = int(x)
            x += r.width + extra_per_gap

        return runs
```

#### 6B：制表位

```python
# layout/tab_stop.py

class TabStopEngine:
    """
    制表位类型：
    - left:    文本从制表位向右
    - center:  文本以制表位为中心
    - right:   文本到制表位结束
    - decimal: 小数点对齐制表位

    默认制表位：每 36pt (0.5 inch) 一个 left tab

    前导符 (leader)：
    - none / dot / hyphen / underscore / heavy
    """

    def resolve_tab(self, current_x: int, tab_stops: list,
                    default_tab: float, scale: float) -> int:
        # 找到下一个大于 current_x 的制表位
        stops = sorted([ts.position * scale for ts in tab_stops])
        if not stops:
            # 使用默认制表位
            default_px = default_tab * scale
            return int((current_x // default_px + 1) * default_px)

        for s in stops:
            if s > current_x:
                return int(s)
        return int(stops[-1] + default_tab * scale)
```

#### 6C：文本框与浮动元素

```xml
w:r → w:pict → v:shape           VML 文本框
w:r → w:drawing → wp:anchor      DrawingML 文本框
├── w:txbxContent                文本框内容
│   └── w:p[]                    段落
├── wp:wrapSquare                环绕
└── a:xfrm                       位置/大小/旋转
```

```python
# layout/float_layout.py

class FloatLayoutEngine:
    """
    浮动元素排版：
    1. 解析锚点位置 (relativeFrom: page/margin/column/paragraph)
    2. 计算浮动元素的绝对坐标
    3. 根据环绕类型计算文字避让区域
    4. 断行时跳过避让区域
    5. 层叠顺序 (z-index): behind < text < inFront

    环绕类型：
    - square:      矩形环绕
    - tight:       紧贴轮廓（简化为矩形）
    - topAndBottom: 上下不环绕
    - behind:      文字下方
    - inFrontOf:   文字上方
    """

    def compute_exclusion_zones(self, floats: list,
                                page_area) -> list:
        """计算每行的可用宽度（排除浮动元素占据的区域）"""
        zones = []
        for f in floats:
            if f.wrap_type in ('square', 'tight'):
                zones.append({
                    'y_start': f.y,
                    'y_end': f.y + f.height,
                    'x_start': f.x,
                    'x_end': f.x + f.width,
                })
        return zones
```

#### P6 验收标准

| 测试项 | 预期 |
|--------|------|
| 两端对齐（中文） | 字间距均匀 |
| 两端对齐（英文） | 词间距均匀 |
| 分散对齐 | 含末尾空间 |
| 制表位 (left/center/right/decimal) | 对齐正确 |
| 制表位前导符 (dot/underscore) | 显示正确 |
| 文本框（固定位置） | 位置正确 |
| 文本框内多段落 | 内容正确 |
| 文字环绕 (square) | 环绕正确 |
| 文字环绕 (top/bottom) | 上下留白 |
| 层叠 (behind/inFront) | 层序正确 |

---

### P7 · 公式 OMML（第 12~13 周）

```xml
m:oMath
├── m:r → m:t "x"              普通字符
├── m:f (分数)
│   ├── m:num → m:r → m:t "a"
│   └── m:den → m:r → m:t "b"
├── m:rad (根号)
│   ├── m:deg                   次数
│   └── m:e → m:r → m:t "x"
├── m:sSup (上标)
│   ├── m:e → m:r → m:t "x"
│   └── m:sup → m:r → m:t "2"
├── m:sSub (下标)
├── m:sSubSup (上下标)
├── m:nary (求和/积分)
│   ├── m:sub / m:sup          上下限
│   └── m:e                    被操作表达式
├── m:d (定界符/括号)
│   ├── m:dPr → m:begChr / m:endChr
│   └── m:e
├── m:m (矩阵)
│   └── m:mr → m:e
├── m:bar (上划线/下划线)
├── m:acc (重音符号)
├── m:func (函数 sin/cos/log)
├── m:eqArr (公式数组)
└── m:limUpp / m:limLow        上下极限
```

```python
# parse/math_omml.py → model/math_ast.py

# AST 节点定义
@dataclass
class MathNode:
    pass

@dataclass
class MathChar(MathNode):
    char: str
    style: str = "p"  # p=plain, i=italic, b=bold

@dataclass
class MathFrac(MathNode):
    numerator: MathNode = None
    denominator: MathNode = None

@dataclass
class MathRad(MathNode):
    degree: MathNode = None     # None = 平方根
    radicand: MathNode = None

@dataclass
class MathSup(MathNode):
    base: MathNode = None
    superscript: MathNode = None

@dataclass
class MathSub(MathNode):
    base: MathNode = None
    subscript: MathNode = None

@dataclass
class MathNary(MathNode):
    char: str = "∑"            # ∑ ∫ ∏ ∮
    lower: MathNode = None
    upper: MathNode = None
    body: MathNode = None

@dataclass
class MathDelim(MathNode):
    open_chr: str = "("
    close_chr: str = ")"
    body: MathNode = None

@dataclass
class MathMatrix(MathNode):
    rows: list = field(default_factory=list)  # [[MathNode]]

# layout/math_layout.py

class MathLayoutEngine:
    """
    公式排版算法：
    1. 递归遍历 AST
    2. 每个节点计算 (width, height, ascent, descent)
    3. 分数：分子分母上下排列，中间画线
    4. 根号：画 √ 符号 + 上划线
    5. 上下标：缩小字号 (约 70%)，偏移位置
    6. 大运算符 (∑∫)：放大字号，上下放极限
    7. 括号：根据内容高度拉伸
    """

    def layout(self, node: MathNode, font_mgr, base_size: float):
        if isinstance(node, MathChar):
            return self._layout_char(node, font_mgr, base_size)
        elif isinstance(node, MathFrac):
            return self._layout_frac(node, font_mgr, base_size)
        elif isinstance(node, MathRad):
            return self._layout_rad(node, font_mgr, base_size)
        elif isinstance(node, MathSup):
            return self._layout_sup(node, font_mgr, base_size)
        elif isinstance(node, MathNary):
            return self._layout_nary(node, font_mgr, base_size)
        elif isinstance(node, MathDelim):
            return self._layout_delim(node, font_mgr, base_size)
        ...

    def _layout_frac(self, node, fm, size):
        num_box = self.layout(node.numerator, fm, size * 0.85)
        den_box = self.layout(node.denominator, fm, size * 0.85)
        w = max(num_box.width, den_box.width) + 8
        h = num_box.height + den_box.height + 6  # 6=分数线+间距
        return MathBox(width=w, height=h, ...)
```

```python
# render/math_renderer.py

class MathRenderer:
    """
    公式绘制：
    - 分数：draw.line() 画分数线
    - 根号：draw.polygon() 画 √ 形状 + draw.line() 上划线
    - 括号：根据高度用字体渲染或 draw.arc()
    - 大运算符：用大号字体渲染 ∑ ∫ ∏
    - 上下标：小字号 + y 偏移
    """

    def render(self, draw, box: MathBox, x: int, y: int):
        ...

    def _draw_radical(self, draw, x, y, w, h):
        """画根号 √"""
        # √ 形状：短竖 → 斜线 → 长竖 → 上划线
        points = [
            (x, y + h * 0.6),
            (x + 3, y + h),
            (x + 8, y),
            (x + 8, y),
        ]
        draw.line(points, fill=(0, 0, 0), width=1)
        draw.line([(x + 8, y), (x + w, y)], fill=(0, 0, 0), width=1)
```

#### P7 验收标准

| 测试项 | 预期 |
|--------|------|
| 行内公式 $x^2$ | 上标正确 |
| 分数 $\frac{a}{b}$ | 上下排列+分数线 |
| 根号 $\sqrt{x}$ | √ 形状正确 |
| n次根号 $\sqrt[3]{x}$ | 次数显示 |
| 求和 $\sum_{i=1}^{n}$ | 上下限正确 |
| 积分 $\int_0^1$ | 正确 |
| 括号自适应 $(...)$ | 高度匹配 |
| 矩阵 | 网格排列 |
| 函数 sin/cos/log | 正体 |
| 公式数组（多行） | 对齐正确 |

---

## 五、字体管理（贯穿所有阶段）

```python
# font/manager.py

class FontManager:
    """
    字体管理策略：

    1. 查找顺序：
       ① 文档嵌入字体 (word/fonts/)
       ② 项目 fonts/ 目录
       ③ 系统字体目录
       ④ 回退字体

    2. 字体回退链：
       指定字体 → 同族字体 → 系统默认 → DejaVu Sans

    3. 中英文分别处理：
       - ASCII 字符 → font_ascii (如 Times New Roman)
       - CJK 字符 → font_east_asia (如 SimSun)
       - 逐字符选择字体

    4. 字体度量：
       - 使用 fontTools 读取 OS/2 表
       - ascent / descent / lineGap
       - 用于精确行高计算

    5. 缓存：
       - LRU 缓存已加载的 ImageFont 对象
       - key = (font_path, size_px)
    """

    SYSTEM_FONT_DIRS = {
        'linux': ['/usr/share/fonts', '/usr/local/share/fonts'],
        'darwin': ['/System/Library/Fonts', '/Library/Fonts',
                   os.path.expanduser('~/Library/Fonts')],
        'win32': ['C:/Windows/Fonts'],
    }

    FALLBACK_CHAIN = [
        'DejaVu Sans',
        'Noto Sans CJK SC',
        'WenQuanYi Micro Hei',
        'Arial',
    ]

    def get_font_for_char(self, ch: str, props: RunProps,
                          size_px: int) -> ImageFont:
        """根据字符选择字体"""
        if self._is_cjk(ch):
            name = props.font_east_asia
        else:
            name = props.font_ascii

        font = self._load(name, size_px, props.bold, props.italic)
        if font and self._has_glyph(font, ch):
            return font

        # 回退
        for fb in self.FALLBACK_CHAIN:
            font = self._load(fb, size_px, props.bold, props.italic)
            if font and self._has_glyph(font, ch):
                return font

        return ImageFont.load_default()

    def _has_glyph(self, font: ImageFont, ch: str) -> bool:
        """检查字体是否包含该字符的字形"""
        try:
            cmap = font.font.charmap if hasattr(font.font, 'charmap') else {}
            return ord(ch) in cmap
        except Exception:
            return True  # 无法判断时假设支持
```

---

## 六、渲染引擎（贯穿所有阶段）

```python
# render/canvas.py

class Canvas:
    """Pillow 画布封装"""

    def __init__(self, width: int, height: int, dpi: int = 150):
        self.dpi = dpi
        self.img = Image.new("RGB", (width, height), (255, 255, 255))
        self.draw = ImageDraw.Draw(self.img)

    def draw_text(self, x, y, text, font, fill, **kwargs):
        self.draw.text((x, y), text, font=font, fill=fill, **kwargs)

    def draw_line(self, x1, y1, x2, y2, fill, width=1, style="solid"):
        if style == "solid":
            self.draw.line([(x1, y1), (x2, y2)], fill=fill, width=width)
        elif style == "dashed":
            self._draw_dashed_line(x1, y1, x2, y2, fill, width, 6, 4)
        elif style == "dotted":
            self._draw_dashed_line(x1, y1, x2, y2, fill, width, 2, 3)

    def draw_rect(self, x1, y1, x2, y2, outline=None, fill=None, width=1):
        self.draw.rectangle([x1, y1, x2, y2], outline=outline, fill=fill, width=width)

    def draw_image(self, img: Image.Image, x, y, w, h):
        resized = img.resize((w, h), Image.LANCZOS)
        self.img.paste(resized, (x, y))
        self.draw = ImageDraw.Draw(self.img)  # 刷新 draw

    def save(self, path, fmt="PNG", quality=95):
        self.img.save(path, fmt, dpi=(self.dpi, self.dpi), quality=quality)
```

---

## 七、CLI 与 API

```python
# cli.py
import argparse
from .api import convert

def main():
    parser = argparse.ArgumentParser(description="DocX → Image (Pure Python)")
    parser.add_argument("input", help="输入 .docx 文件")
    parser.add_argument("-o", "--output", default="./output", help="输出目录")
    parser.add_argument("--dpi", type=int, default=150, help="DPI (72-600)")
    parser.add_argument("--format", choices=["PNG", "JPEG", "TIFF"], default="PNG")
    parser.add_argument("--pages", help="指定页码, 如 1,3,5-8")
    parser.add_argument("--quality", type=int, default=95, help="JPEG 质量")
    parser.add_argument("--font-dir", help="额外字体目录")
    parser.add_argument("-v", "--verbose", action="store_true")
    args = parser.parse_args()

    paths = convert(
        args.input, args.output,
        dpi=args.dpi, fmt=args.format,
        pages=args.pages, quality=args.quality,
        font_dir=args.font_dir, verbose=args.verbose,
    )
    for p in paths:
        print(f"✅ {p}")

# api.py
def convert(input_path, output_dir="./output", dpi=150,
            fmt="PNG", pages=None, quality=95,
            font_dir=None, verbose=False) -> list:
    """
    主转换函数

    >>> from docx2img import convert
    >>> convert("report.docx", dpi=300)
    ['output/page_000.png', 'output/page_001.png']
    """
    ...
```

```bash
# 使用
$ docx2img report.docx -o ./images --dpi 300 --format PNG
$ docx2img report.docx --pages 1,3,5-8
$ python -c "from docx2img import convert; convert('a.docx')"
```

---

## 八、测试策略

```
tests/
├── unit/                        单元测试
│   ├── test_units.py            单位换算
│   ├── test_parse_paragraph.py  段落解析
│   ├── test_parse_table.py      表格解析
│   ├── test_parse_drawing.py    图片解析
│   ├── test_parse_math.py       公式解析
│   ├── test_style_resolver.py   样式继承
│   ├── test_line_breaker.py     断行算法
│   ├── test_page_breaker.py     分页算法
│   ├── test_table_layout.py     表格布局
│   ├── test_numbering.py        编号引擎
│   └── test_justify.py          两端对齐
│
├── integration/                 集成测试
│   ├── test_parse_full.py       完整文档解析
│   └── test_layout_full.py      完整排版
│
├── e2e/                         端到端测试
│   ├── test_basic_text.py       基础文本 → 图片
│   ├── test_tables.py
│   ├── test_images.py
│   ├── test_headers_footers.py
│   ├── test_lists.py
│   ├── test_columns.py
│   ├── test_math.py
│   └── test_complex.py          混合复杂文档
│
├── visual/                      视觉回归测试
│   ├── baseline/                基准图片 (人工确认)
│   ├── test_visual_diff.py      像素级对比 (SSIM / MSE)
│   └── generate_baseline.py
│
└── fixtures/                    测试文档
    ├── generated/               脚本生成的 .docx
    │   └── gen_fixtures.py      用 python-docx 生成测试文件
    └── real_world/              真实文档
```

```python
# tests/visual/test_visual_diff.py
from skimage.metrics import structural_similarity as ssim
import numpy as np
from PIL import Image

def test_basic_text_visual():
    """视觉回归：基础文本"""
    result = render_to_image("fixtures/basic_text.docx")
    baseline = Image.open("baseline/basic_text.png")

    arr_r = np.array(result.convert("L"))
    arr_b = np.array(baseline.convert("L"))

    score = ssim(arr_r, arr_b)
    assert score > 0.95, f"SSIM={score:.4f}, 低于阈值 0.95"
```

---

## 九、性能目标与优化

| 指标 | 目标 | 优化手段 |
|------|------|---------|
| 10 页纯文本 | < 2s | 字体缓存、批量绘制 |
| 50 页含表格 | < 10s | 延迟解析、分页渲染 |
| 100 页复杂文档 | < 30s | 多进程 (ProcessPool) |
| 内存占用 | < 500MB | 逐页渲染、及时释放 |
| 字体加载 | 首次 < 1s | LRU 缓存、预加载 |

```python
# 多页并行渲染
from concurrent.futures import ProcessPoolExecutor

def render_parallel(pages, section, dpi, max_workers=4):
    with ProcessPoolExecutor(max_workers=max_workers) as pool:
        futures = [
            pool.submit(_render_single_page, page, section, dpi)
            for page in pages
        ]
        return [f.result() for f in futures]
```

---

## 十、迭代里程碑总览

| 阶段 | 周期 | 交付物 | 还原度 |
|------|------|--------|--------|
| **P0** 基础文本 | W1-W2 | 纯文本 docx → PNG | ~60% |
| **P1** 样式系统 | W3 | 样式/主题/继承 | ~70% |
| **P2** 表格 | W4-W5 | 复杂表格/合并/嵌套 | ~78% |
| **P3** 图片+分节分栏 | W6-W7 | 图片/环绕/多节/分栏 | ~83% |
| **P4** 页眉页脚 | W8 | 页眉/页脚/页码/域 | ~86% |
| **P5** 列表编号 | W9 | 多级列表/编号格式 | ~89% |
| **P6** 高级排版 | W10-W11 | 两端对齐/制表位/文本框/浮动 | ~93% |
| **P7** 公式 | W12-W13 | OMML 公式渲染 | ~95% |
| **持续** 优化+修复 | W14+ | 性能/视觉回归/边界case | →97% |

> **最终预期还原度**：简单文档 ~97%，复杂文档 ~90-93%。剩余差距主要来自 Word 未文档化的排版细节和字体渲染引擎差异，这是纯 Python 方案的理论天花板。

---